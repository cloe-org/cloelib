# =====================================================================
# cloe-org – Conda/Mamba Environment
# ---------------------------------------------------------------------
# Purpose:
#   - smoothly run cloe-org ecosystem
#
# How to use:
#   mamba env create -f cloelib-env.yaml
#   mamba activate cloelib-env
#
# Notes:
#   - Python 3.12
#   - pip section at the end for Euclid-specific and sampling packages
# =====================================================================

name: cloelib-env

# -----------------------------
# Conda channels
# -----------------------------
channels:
  - conda-forge
  - nodefaults
# -----------------------------
# Core dependencies
# -----------------------------
dependencies:
  # --- Low-level runtime & threading ---

  # --- Python runtime ---
  - python=3.12
  - cpython
  - pip
  - setuptools
  - wheel

  # --- GSL & numerical utilities ---
  - gsl
  - libgfortran5

  # --- C++ / compiler toolchain ---
  # Needed for pybind11, Boost, and compiled extensions
  - gcc>=15

  # --- Interactive & debugging tools ---
  - ipython
  - ipykernel
  - jupyter

  # --- Plotting / UX helpers ---
  - matplotlib

  # -----------------------------
  # pip-only dependencies
  # -----------------------------
  # Euclid- and project-specific packages not available via conda
  - pip:
      - euclidlib==2026.1
      - nautilus-sampler==1.0.6
      - seaborn
      - pre-commit
# ============================================================
# End of environment definition
# ============================================================
